apply from: 'gradle/gradle-android-utils/build.gradle'

ext {
    githubProjectName = 'Rebuild'
    versions = [
            findbugs: '2.0.0',
    ]
}

allprojects { project ->
    buildscript {
        repositories {
            mavenCentral()
        }
    }
    repositories {
        mavenCentral()
    }
}
